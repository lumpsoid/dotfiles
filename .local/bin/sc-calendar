#!/bin/sh

if [[ "$1" == "-c" ]]
    then
        remind -cb1m ~/.config/remind/uni.rem
        sleep 5
        exit 1
fi

if [[ "$1" == "-e" ]]
    then
        nvim ~/.config/remind/uni.rem
        exit 1
fi

